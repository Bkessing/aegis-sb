// MCP server entry point. Stub in v0.1 — full implementation lands in v0.2.
// The bin shim exists today so installs configure correctly; running it
// before v0.2 ships prints an explanatory message.

console.error("aegis-sb MCP server is not yet available (lands in v0.2).");
console.error("Use the CLI for now: npx aegis-sb <project-url> --key <anon-key>");
process.exit(1);
